spring:
  application:
    name: auth

  datasource:
    url: jdbc:mysql://localhost:3306/birthday?&serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: password

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update

  data:
    redis:
      host: localhost
      port: 6379
      password: password

# Todo : raw_key 변경 후 환경변수 처리
jwt:
  expiration-minutes: 15
  expiration-days: 7